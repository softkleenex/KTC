DAY1

동기(앞 작업의 결과와 뒤 작업의 시작을 맞춘다)

비동기(앞 작업의 결과와 뒤 작업의 시작을 맞추지 않는다) 
	동시에 여러 연산을 하는것이 아니라, 대기 상태에 빠진 작업이 다른 작업에게 실행 흐름을 양보하는구조

동기와 비동기의 사전적 의미와 혼동하지말것


프로세스 : 실행중인 프로그램의 인스턴스
	독립적인 메모리

스레드 : 프로세스 내 실행작업의 단위
	메모리 공유
	하나의 프로세스 = 하나 이상의 스레드

파이썬에서는 비동기 프로그램 구성이 힘들다, GIL(Global Interpreter Lock가 그것을 막고 있기 때문이다.)

GIL은 한번에 하나의 스레드만 파이썬 코드를 실행하도록 제한하는 역할을 가진다.

GIL에 의해서 스레드를 아무리 늘려도 CPU연사ㄴ이 병렬로 빨라지지는 않는다. 하지만 비동기는 기다리는동안 권한을 넘기므로, 단일 프로세스 만으로 IO에서 큰 이득을 본다
(파이썬이 아니라 다른 작업은 파이썬이 기다리는동안 작업될 수 있으므로)


---

파이썬에서 비동기 작업을 하려면…?
import asyncio (파이썬에서의 비동기 프로그래밍을 하기 위한 작업)

비동기 세계의 핵심 계층(안쪽부터)

Coroutine(코 루틴)

비동기로 작동하는 함수 그 자체

Task (태스크)
루프에 예약되어 실행 대기중인 작업

Event Loop(루프)
모든 비동기 작업을 지휘하는 중앙 엔진



비동기 함수는 어떻게 만드는가…?

async + def로 만든다. 하지만 def로 만드는 일반함수와는 달리, 호출시에 바로 실행되는게 아니라 비동기적으로 실행될 수 있는 상태인 코루틴을 반환한다.


차이점1. 비동기 함수 안에서는 await을 사용할 수 있다
차이점2. 호출시에 실행되는게 아니라 코루틴 객체가 반환됨(class : coroutine)
	호출하고 실제로 실행하지않으면 RuntimeWarning 발행


그러면 어떻게 실행해…?

---
await에 대해서도 알아보자

이런식으로! coroutine 앞에다가 하면된다.
`await`은 **코루틴을 즉시 '실행'시키고, 그 작업이 끝날 때까지** 기다리는 동안 제어권을 이벤트 루프에 양보하는 것. (즉, 실행 버튼과 일시정지 버튼을 동시에 누르는 효과!)
일반함수에다가 사용시 syntaxerror발생하니 유의할것!


await asyncio.sleep(초)

일반 sleep은 모든 작업 종료, 하지만 asyncio sleep는 자신의 작업만 멈추고, 제어권을 이벤트 루프에 양보하는것.


실행하는건? asyncio.run(coroutine) = coroutine의 실행 결과값.

새 이벤트 루프 생성 > 코루틴 실행 > 루프 종료
프로그램에서 딱 한번만 호출!
이미 루프가 실행중일떄 asyncio.run()하면 RuntimeError 발생


즉, 비동기 환경을 생성하고 첫 작업을 구동하는 엔진의 시동 키 같은역할.


---



Awaitable객체의 핵심
	Coroutine async def 함수를 호출하면 생성되는 객체이다, 스스로 실행될 수 없는 잠재직 작업.
	task (Future을 상속하긴했지만, 직접 다룰일이 거의 없기 때문에, await으로 결과를 받을수 있는 비동기 작업 단위 정도로 하자)
	Future(내구 구현에 사용되는 저수준 객체, 직접 다룰일은 거의 없음)


create_task으로 즉시 백그라운드 실행도 가능!

asyncio.create_task(코루틴 객체, 즉  코루틴 함수의 호출 결과) = Task 객체

조건: 실행중인 이벤트 루프가 필요하다. 즉, async 함수 안에서만 가능하다.


await 코루틴()

await을 만나야 실행시작
순차적으로 실행됨, 하나가 끝나야 다음으로 진행
await 가 즉시 결과를 반환한다
완료까지 현제 코루틴에 제어권이 대기된다
단일 작업 순서대로 처리된다

create_task
creat_task호출 즉시 등록(루프에 여유가 생기면 실행된다)
동시에 실행된다 - 백그라운드에서 진행
await task로 나중에 결과를 수집하여 결과를 받는다
제어권 측면에서는 등록 이후 바로 다음코드가 실행된다
여러 작업을 동시에 처리할떄 사용된다

---

여러 비동기 잡업을 동시에 처리하려면 어떻게 해야할까?

1초, 2초, 5초 작업이 있다면

원래대로라면 1 + 2 + 5 = 8초
하지만 동시에 달리게 한다면 max(1, 2, 5) = 5

이런 역할을 하는건, 여러 작업을 한번에 실행해서 결과를 전달해주는 수집자, gather

await asyncio.gather(*코루틴들)

ex

await asyncio.gather(
fetch("api/1"),
fetch("api/2"),
fetch("api/3")
)

주목 포인트 : 완료 순서와 무관하게 결과 리스트는 입력순서와 항상 같다.


순차실행과 동시실행의 비교

![[Pasted image 20260522000513.png]]


비동기 라이프 사이클


시작(start) asyncio.run() 이벤트 루프 구동

실행(Execute): await / gather() 작업 지시 및 제어권 양보

생성(Create) : async def 코루틴 객체 준비


---

taskcroup은 마치 울타리 같은 역할을 한다.

async for 비동기 이터레이터를 순회

asycn with 비동기 컨텍스트 매니저



asyncio.run 모든 비동기 작ㅓㅂ의 최초 진입점

대기 / 양보 await 순차적 실행 및 제어권 반환

동시실행 asyncio.gather 병렬처리(가장 긴시간에 동기화)

백그라운드 create_task 대기없이 즉시 실행예약




---
gather으로 여러 작업을 동시에 실행하면 하나가 실패한다면...?

에러 발생기의 기본동작 : 예외가 전파됨.(Exception Propagation)

기본 동작에서는 하나의 예외가 발생시에 즉시 호출자에게 전파되고, 나머지 태스트는 취소되지 않고 계속 실행된다.

예외를 외부가 아니라 안에서 쓰고 싶다면?

return_exceptions = True 옵션을 써라!

예외를 반환 가능한 결과값으로 취급하게끔 시스템의 규칙을 변경한다.

![[Pasted image 20260524004819.png]]

이러면 뭐가 좋냐

1. 작업별 상태 확인 > 각 작업의 성공 / 실패를 따로 확인 가능
2. 선택적 재시도 > 실패한 작업만 골라서 다시 실행 가능
3. 부분 성공 활용 > 성공한 결과는 먼저 활용 가능


![[Pasted image 20260524005034.png]]

---


asyncio.wait_for(코루틴, timeout = 초)

time 초과시에 asyncio,TimeoutError 발생함

여기서 핵심 포인트는 시간 내에 완료 시에 정상 결과 반환
시간초과시에는 태스트 자동취소 + TimeoutError 발생

try.except로 안전하게 처리하는것이 필수이다.


time out은 꼭 필요하당!!!

없으면 전체 시스템 흐름 꼬이고, 사용자 경험 악화됨

있으면 빠른 피드백 제공 / 시스템 리소스 보호됨


---

한번 시작한 작업이 필요없다면,,, 시스템 자원 확보를 위해서 즉시 중단해야함!!!


작업 중단은 Cancel() 사용

task.cancel()으로 취소 요청시에 await에서 CancelledError 발생함

try - except 보통 사용

| 에러                   | 발생 상황                          | 대처법                                  |
| :------------------- | :----------------------------- | :----------------------------------- |
| **`TimeoutError`**   | `wait_for()`에서 시간 초과           | `try/except`로 포착, 대체 로직 실행           |
| **`CancelledError`** | `task.cancel()`로 태스크 취소        | `try/except` 또는 `finally`에서 정리 작업    |
| **`RuntimeError`**   | 이미 루프 실행 중에 `asyncio.run()` 호출 | 중첩 호출 금지, `async` 함수 안에서는 `await` 사용 |
| **`ExceptionGroup`** | `TaskGroup` 내 여러 태스크에서 예외 발생   | `except*` 문법으로 타입별 분리 처리 (3.11+)     |
| **`RuntimeWarning`** | 코루틴을 `await` 없이 그냥 호출          | 반드시 `await` 또는 `create_task`로 실행     |



하지만,, 이정도로는 아직 위험하다

Semaphore : 동시 실행 수 제한하기.

sem = asyncio.Semmaphore(최대의 수)

으로 세마포어를 지정해주고

async with sem: 으로 진입한다.

자리가 있으면 진입, 없으면 대기하는방식.





비동기에서 다루는 네트워크 클라이언트나 데이터 베이스 연결 객체같은건,,, 제대로 닫아줘야한다! 단순 변수가 아니라 물리적 자원이기 떄문에, 제대로 비동기로 열고 비동기로 닫아 주자.


async with 이라는 보호막 덕분에, 자원정리(Clean up)을 보장해준다.


| 상황 (Situation)  | 초보자의 접근 (Basic Async) | 실무자의 제어 (Managed Async) |
| :-------------- | :-------------------- | :---------------------- |
| 더 이상 필요 없는 작업   | 끝날 때까지 방치             | `cancel()` 호출 및 롤백      |
| 10,000개의 API 요청 | 한 번에 모두 실행            | `Semaphore`로 동시성 제한     |
| 네트워크 연결 사용      | 열어두고 방치함              | `async with`로 자동 정리     |

비동기 프로그래밍에 대한 유의사항

만능열쇠가 아니고, 전체 흐름이 일관적이여야하고, 흐름이 복잡해져 검증 또한 복잡해진다

유의 사항들

await 누락 - 코루틴 객체가 생성되고 실제로는 실행되지않는 현상

asyncio.run(main(), debug = True) 로 해결 !


동기 sleep 사용 - 이펜트 루프 전체를 멈춰버리는 가장 큰 실수

time.sleep(1)
시스템 전체 정지

await asyncio.sleep(1)
제어권 양보(1초기다릴 테니 다른 작업 먼저 하세용)


정말로 동기식 블로킹 함수를 써야한다면(비동기 지원이 아예 안 되는 남의 코드를 써야 하거나, 사진 압축처럼 CPU를 미친 듯이 갈아 넣어야 하는 무거운 작업을 만나면)
run_in_executor 을 사용해서 블로킹 함수를 메인 루프가 아니라 별도의 스레드 풀로 격리해서 실행해서 해결!


await loop.run_in_executor(executor, 동기함수)

인자1: executor(None = 기본 스레드 풀)
인자2 : 동기함수 참고(() 없이 !)
반환값: 동기함수의 실행 결과값

핵심 Points
	동기함수를 별도 스레드에서 실행 -> 루프 블로킹 방지
	비동기 전환이 안되는 레거시 코드 대처용
	ProcssPoolExecutor 전달시에 프로세스 풀로도 가능






동기 라이브러리 혼용 - 겉모습만 비동기, 실제로는 병목을 유발하는 구조

진정한 성능 향상을 위해서는 I/O가 발생하는 모든 지점(네트워크, 파일 DB)을 비동기 전용 라이브러리로 교체해야한다!!!

비동기 성능의 진실과 오해

단일 요청에 대한 처리 속도를 높히는게 아니라, 동시처리하는 요청의 수를 높히는것이다.


파이썬 비동기 프로그래밍은 코드들끼리 서로 협력하는 정교한 오케스트레이션!!! 이걸 해보장

---

테스트란? 어떤 입력을 넣었을떄 기댛ㄴ 결과가 정확히 나오는지 코드로 검증하는 과정



테스트의 종류
- 단위 테스트(Unit Test)
	- 개별 코드 단위 검증
	- 격리된 환경에서 빠르게 실행
	- 회귀 테스트의 기반
- 통합 테스트(Integration Test)
	- 모듈 간 상호작용 검증
	- 데이터 흐름 및 인터페이스 확인
	- 종속성 및 외부 시스템 통합 확인
- 시스템 테스트(System Test)
	- End-to-end  전체 시스템 기능 검증
	- 실체 운영 환경과 유사한 조건
	- 성능 부하 스트레스 테스트 포함
- 인스 테스트(AcceptanceTest)
	- 최종 사용자 관점의 검증
	- 비즈니스 요구사항 충족 확인
	- 알파, 베타 테스트 및 인수 기준 검증
	- 

왜 테스트 코드를 작성하나

문제점 조기 발견, 리팩토링 용이성, 개발 효율성의 극대화


---

네트워크의 핵심개념


네트워크란

두대 이상의 컴퓨터를 연결하여 네이터를 공유할 수 있도록 연결된 통신시스템

네트워크의 기본요소

1. 클라이언트
	서비스를 요청하는 컴퓨터 또는 사용자
2. 서버
	클라이언트의 요청에 응답하여 서비스를 제공하는 컴퓨터
3. 통신 프로토콜
	네트워크 상에서 데이터를 교환하기 위한 규칙과 절차


IP주소

데이터 패킷의 발신지와 목적지를 식별
네트워크 통신을 가능하게 함

4개의 옷텟(8비트)로 구성, 각 옥텟은 0 -- 255 범위의 숫자, 약 43억개의 고유 주소 제공 가능

IP주소는 다시 네트워크 ID(네트워크 자체를 식별) 와 호스트 ID로 구분가능(네트워크 안의 개별 장치 식별)


![[Pasted image 20260524125735.png]]


이 네트워크 ID와 호스트ID를 구분하는 구분자를 서브넷 마스크라 한다!!!


그런데,,, 지금까지의 체계(IPV4)로는 주소가 부족하다는 한계가 있다(43억개는 너무 작아 ㅠㅠ)


그래서, 128 비트(IPv6)체계로 넘어갔다. 자동 구성 및 주소 중복 방지, 향상된 보안기능이 강점


IP주소를 확인하는방법

명령어 프롬프트에 nslookip 옵션 도메인 이름 입력으로 확인가능




DNS

IP주소 자체의 한계 : IP주소는 너무 길고, 주소가 변경 가능하다.

이를 보완하기 위해 등장한것이 기억하기 좋은 도메인 이름을 쓴다!(52.231.38.95 > www.elice.io) 

이떄 필요한것이 Domain Name System(DNS) 도메인 이름 <> ip주소를 매칭해주는 전화기록부 같은것

도메인 이름을 입력하면 DNS서버가 실제 IP주소로 반환한다.


범위는 0 ~ 65525 까지의 숫자
IP주소 뒤에 : 으로 표기
중복 불가능

지정 포트

| 포트 번호 | 용도 |
| :--- | :--- |
| **21** | FTP (File Transfer Protocol) |
| **22** | SSH (Secure Shell) |
| **23** | TELNET |
| **25** | SMTP (Simple Mail Transfer Protocol) |
| **53** | DNS (Domain Name System) |
| **80** | HTTP (HyperText Transfer Protocol) |
| **443** | HTTPS (HTTP의 보안이 강화된 버전) |

<br>

**📌 웰-노운 포트(Well-known Ports)**
- **0 ~ 1023**
- 표준 서비스와 프로토콜에 예약된 포트 번호


PORT 번호

만약 같은 기기에 둘 이상의 기기가 연결되면 어떻게 통신해야할까?

네트워크에서 컴퓨터나 특정 애플리케이션 또는 서비스에 연결할 수 있도록 식별하는 번호
동일한 IP 주소를 사용하는 여러 서비스 간의 데이터 전송을 구분한다.


네트워크의 종류

작은 범주의 네트워크부터

PAN(Personal Area Network)
개이느이 장치들간의 연결을 위한 소규모 네트워크(스마트폰, 무선이어폰 등)

LAN(Local Area Network)
제한된 지역 내의 컴퓨터와 장치들을 연결한 네트워크(사무실, 학교, 가정 내 PC등)


MAN(Metropolitan Area Network)
도시나 대도시 지역을 커버하는 네트워크(도시 내 공공와이파이, 캠퍼스 네트워크)

WAN(Wide Area Network)
넓은 지리적 범위를 커버하는 네트워크(인터넷, 글로벌 기업 네트워크)



네트워크 토폴로지와 통신방식

네트워크 토폴로지 : 컴퓨터 네트워크의 요소들(링크, 노드 등)을 물리적으로 연결한 구조 및 연결방식
![[Pasted image 20260524131501.png]]
- 버스 토폴로지
	- 모든 장치가 하나의 중앙 케이블(백본) 에 연결
	- 설치가 쉽고 비용이 저렴함, 케이블 문제 발생시에 전체 네트워크에 영향
	- 초기 이더넷 네트워크 연결방식
![[Pasted image 20260524131511.png|409]]
- 스타 토폴로지
	- 모든 장치가 중앙 허브 혹은 스위치에 연결
	- 중앙 장치로 인해 관리용이
	- 중앙 장치의 장애가 전체 네트워크
![[Pasted image 20260524131555.png|256]]
- 링 토폴로지
	- 각 장치가 두 인접 장치와 연결되어 링 형태를 이룸
	- 데이터 충돌이 적지만 하나의 장치 장애가 전체 네트워크에 영향
	- FDDI 네트워크 사용방식(FDDI = "광케이블을 써서 빠르고, 링을 두 겹(이중 링)으로 만들어서 한 놈이 고장 나도 절대 안 터지는 튼튼한 링 네트워크")
![[Pasted image 20260524150744.png]]
- 메시 토폴로지
	-  모든 장치가 서로 직접 연결
	- 장애 발생시 대체 경로 제겅 -> 높은 신뢰성과 확장성
	- 설치와 유지관리 비용이높음



네트워크 통신방식

네트워크에서 데이터를 전송하는방법
데이터 전송의 효율성, 신뢰성, 속도 등에 영향을 미친다.

1. 유니 캐스트
	-  한 송신자와 수신자가 1:!로 통신함
	- 네트워크 리소스 효율적 사용
	- 신뢰성 높은 통신방식
		- 웹 브라우징(HTTP, HTTPS), 이메일 전송, 파일 전송 프로토콜)FTP
2. 멀티캐스트
	- 한 송신자가 여러 수신자에게 1:N으로 통신하는 방신
	- 특정 그룹의 수신자에게만 데이터 전송
	- 네트워크 자원 절약 및 효율적인 데이터 분배
		- 라이브 스트리밍, 온라인 강의 네트워크 게임 등에 활용된다
3. 브로드 캐스트
	- 한 송신자가 네트워크 내 모든 대상과 통신하는 방식
	- 네크워크 내 다른 장치들을 발견하고 서비스 제공에 용이함
	- 네트워크 트래픽 증가 가능성있음
		- ARP요청(**"IP 주소를 MAC 주소로 바꿔주는 통역사(또는 명부)"** 역할을 하는 프로토콜입니다.), 네트워크 디스크버리 등





TCP/IP 프로토콜

이전에,, IP프로토콜이 가지는 한계에 대해서 알아보자

IP프로토콜이란? 데이터를 목적지까지 전달하는데 사용되는 핵심 프로토콜, IP주소를 통해 송수신자를 식별해 데이터를 패킷 단위로 전송함
한계는? 신뢰성의 문제가 있다.

핵심 발생 지점 : 단순 데이터를 전달하기만 하고, 전달 과정에서ㄴ의 패킷 손실, 순서뒤바뀜에 책임을 지지않음
	데이터 패킷이 독립적으로 전송되어 순서가 보장되지않음
	패킷 손실시 자동 재전송 혹은 복구 기능이없음

TCP/IP프로토콜은 데이터가 정확한 목적지에 도달할수 있도록 하는 통신규약이다. 인터넷 및 네으워크 통신의 기본 프로토콜 스택(IP 에 TCP를 컬합)

네트워크 통신이 일어나는 과정을 4가지 단계로 구분한다



### OSI 7계층 모델
| 계층 | 이름 |
| :--- | :--- |
| **7계층** | 응용 계층 |
| **6계층** | 표현 계층 |
| **5계층** | 세션 계층 |
| **4계층** | 전송 계층 |
| **3계층** | 네트워크 계층 |
| **2계층** | 데이터링크 계층 |
| **1계층** | 물리 계층 |


### TCP/IP 모델
| 계층 | 이름 |
| :--- | :--- |
| **4계층** | 응용 계층 |
| **3계층** | 전송 계층 |
| **2계층** | 인터넷 계층 |
| **1계층** | 네트워크 액세스 계층 |

---

**📌 핵심 설명**
* **전통적인 OSI 7계층 모델**을 실무 환경에 맞게 압축한 **실용적인 구조가 TCP/IP 4계층 모델**입니다.
* **계층 간 대응 관계:**
  * OSI의 **5, 6, 7계층** ➡️ TCP/IP의 **4계층(응용 계층)** 하나로 통합
  * OSI의 **4계층** ➡️ TCP/IP의 **3계층(전송 계층)**
  * OSI의 **3계층** ➡️ TCP/IP의 **2계층(인터넷 계층)**
  * OSI의 **1, 2계층** ➡️ TCP/IP의 **1계층(네트워크 액세스 계층)** 하나로 통합



TCP와 UDP

TCP의 특징
- 연결 지향
	- 3-way handshaking 과 4-way handshaking (통신 전에 연결하는것, 통신 후에 연결을 끊는것)
- 흐름 및 혼잡 제어, 순서를 보장한다
- 높은 신뢰성을 보장한다
- UDP보다 느린 속도
- 이메일 및 파일 전송, 웹페이지 로딩에 쓰임

3way handshaking

user이 serverdptp SVN 메세지를 보내 연결요청
서버는 SYN/ACK 를 동시에 보내 연결 수락
user은 server에세 ACK를 보내 연결 성공


4way handshaking

user이 server에세 fin을 보냄
server에서 user에게 ack보냄
	fin보냄
user이 server에게 ack 보냄

UDP의 특징

- 비 연결형 지향(3way, 4way handshaking 같은거 없음)
- 낮은 신뢰성, 순서 보장 X
- 정보의 신호 절차를 거치지않는다
- 스트리밍, 온라인 게임 등 실시간 서비스에 적합하다.


|       구분        |   TCP    |      UDP      |
| :-------------: | :------: | :-----------: |
|    **연결 방식**    |   연결형    |     비연결형      |
|    **전송 순서**    | 전송 순서 보장 |  전송 순서 보장 X   |
|  **수신 여부 확인**   |    O     |       X       |
|    **통신 방식**    |   1:1    | 1:1, 1:N, N:N |
|     **신뢰성**     |    높음    |      낮음       |
| **오류 검출 및 재전송** |    O     |       X       |
|     **속도**      |    느림    |      빠름       |


---
HTTPS 프로토콜과 RESUFUL

브라우저는 어떻게 작동할까?

1. url을 입력하면 dns서버에서 해당 도메인을 검색하여 ip주소를 반환한다.

2. 브라우저 http 요청 메세지가 생성된다
		이 단계는 어느계층일까? 사용자가 입력에 따라 데이터가 생성되는, 가장 위의 응용계층에서 작동한다.
3. TCP/IP패킷 생성
		이 데이터를 패킷으로, 이 패킷을 프레임으로 포장하여 서버에게 잘 도착하게한다


TTP 메시지가 **TCP 세그먼트로 1차 포장되고, 그게 다시 IP 패킷에 싸인 뒤**, 최종적으로 **프레임(Frame)으로 캡슐화되어** 랜선(물리 계층)을 타고 서버에게 보내진다!

4. 이후 이 프레임이 서버가 있는곳으로 도착하면, 이 프레임을 패킷으로, 이 패킷을 데이터로 해체하여 내용을 확인한다.(역 캡슐화)
5. 이제 이 응답에 대해서 서버가 대답해야한다. 문제 여부와 상관없이 항당 사용자에게 메시지로 응답해야한다.
	- 예를들면, https, json, javascript 등으로 응답해야한다.
6. 이 과정 이후 최종적으로 화면을 사용자에게 보여주게된다.


HTTP프로토콜과 핵심 특징

프로토콜 : 통신의 과정에 필요한 형식, 약속, 규칙 등을 정의한것
- 네트워크 통신에서 데이터를 교환하기 위한 규칙과 표준 제공
- 데이터 전송의 일관성 보장
- 다양한 기기와 시스템 간의 상호 운용성 제공
- 오류 검출 및 수정기능 제공

프로토콜의 기능


| 기능 | 역할 |
| :--- | :--- |
| **단편화/재합성** | 데이터 블록을 나누고 합쳐서 전송 |
| **캡슐화** | 프로토콜에 필요한 데이터 정보를 부착 |
| **연결/흐름/오류 제어** | 데이터 전송에 필요한 연결/흐름/오류를 개설·조정·종결 |
| **순서 결정** | 데이터 단위 순서대로 수신 측에 전달 |
| **주소 설정** | 발생지, 목적지 등의 주소를 명기하여 데이터를 전달 |
| **동기화/다중화** | 상태를 일치시키는 기능/통신로를 나누어 동시에 사용할 수 있도록 하는 기능 |

왜 HTTP프로토콜이 중요한걸까?

HTTP : HyperText Transfer Protocol
클라이언트 - 서버 간 데이터가 교환되는 방식에 대한 규약
거의 모든 형태의 데이터 전송 가능
무상태성과 비연결성


무상태성
- 서버가 클라이언트의 상태를 보존하지않음
- 각 요청은 독립적이고, 이전요청과의 연결성이없음
- 서버의 확장성에 용이

이게 왜 좋은가?
- 여러 서버중에 아무에게나 요청 가능
- 설계가 단순하고, 수평적 확장성을 높일 수 있다
- 요청이 많아지면 그냥 점원의 수를 늘려 문제해결가능



비연결성
- 클라이언트 - 서버 간연결을 유지하지않음(응답과 동시에 연결이 지속되지않고 종료됨)
- 서버의 불필요한 연결을 줄이고 많은 클라이언트의 요청을 동시에 처리 가능

이게 왜 좋은가?
- 연결이 만약 지속된다면 다른 요청은 응답이 어렵다(사버 자원의 효율화에 유리함)
- 서버의 불필요한 연결을 줄이고 많은 클라이언트의 요청을 통시 처리 가능.

HTTP프로토콜의 연장인 HTTPS(HTTP + S-secure)

HTTPS는 HTTP의 보안버전이다
데이터 암호화로 데이터 보호 및 신뢰성을 향상한다
TLS/SSL 프로토콜을 사용한다.


---
HTTP 메시지와 HTTP 메서드

HTTP 메시지

웹상 클라이어트 - 서버 간데이터 전송형식의 규정
시작 라인, 헤더, 공백, 본문으로 구성
요청 메시지/응답 메시지에 따라 담기는 내용의 차이 존재

HTTP 메서드

클라이언트가 서버에게 수행할 작업의 종류를 지시하는 명령어
RESTful API설계에서 중요한 역할을 한다.


CRUD
	Create > Post(자료를 등록할 떄 사용)
	Read > Get(자료를 조회할 떄 사용)
	Update > Put/Patch(자료를 수정할 떄 사용)
	Delete > Delete(자료를 삭제할 떄 사용)
4가지 기본 기능의 약어, HTTP메서드의 4가지 기능과 대응됨

GET 메서드
- 서버에서 리소스 요청, 데이터 조회
- 데이터 검색, 웹페이지 로딩 등에 사용
- 요청 본문 없음
- 요청 URL에 쿼리 파라미터를 포함할 수 있다.

POST 메서드
- 서버에 데이터를 전송하여 새로운 데이터를 생성
- 폼 제출, 파일 업로드, 회원 가입 등에 사용
- 요청 본문에 데이터 포함
- 서버의 상태를 변경함

PUT 메서드
- 서버에 데이터를 전송하여 리소스를 완전히 대체함
	- PATCH메서드는 일부 대체함
- 데이터 전체 갱신시에 사용
- 요청 본문에 데이터 포함됨

Delete 메서드
- 서버에서 특정 리소스를 삭제함
- 요청 본문 없음(GET 메서드와 동일이유, 리소스를 삭제하는데이 특정 추가 데이터가 필요없기에)


그 외 메서드들

HEAD 메서드 GET 요청과 유사, 응답 본문 포함 X
OPTIONS 메서드 서버가 지원하는 HTTP 메서드 목록 요청
TRACE 메서드 리소스에 대한 경로를 다라 메시지 루프백 메시지 수행

HTTP 요청 메시지 - 응답 메시지

요청 메시지 응답 메시지 모두

Start line/Status line , Header, Blank line, Body로 구성함(첫 요소의 이름 외에는 차이점 x)

요청 메시지의 구성요소

Start line

POST/api/data HTTP/1.1

HTTP 메서드 / 요청 타겟 / HTTP 버전으로 구성됨

HTTP 메서드
- 클라이언트가 서버에게 요청하는 작업의 종류를 표현
요청 타겟
- 클라이언트가 접근하고자 하는 자원의 경로를 표현
HTTP버전

Header

요청에 대한 부가정보를 제공
헤더의 각 속성은 이름-값쌍으로 설명되며 콜론으로 구분
일반헤더, 요청헤더, 응답헤더로 구분
### HTTP 주요 요청 헤더 (Request Headers)

| 헤더 필드 | 설명 |
| :--- | :--- |
| **`Host`** | 요청하는 호스트의 이름 및 포트 번호 |
| **`User-agent`** | 클라이언트가 사용하는 웹 브라우저 정보 (예: 크롬, 사파리 등) |
| **`Referer`** | 현재 요청된 페이지로 이동하기 직전에 방문한 웹 페이지의 URL |
| **`Accept`** | 클라이언트가 처리할 수 있는 데이터 타입 및 우선순위 |
| **`If-Modified-Since`** | 값으로 지정한 날짜 이후에 수정된 자원 요청 (캐시 활용 시 사용) |
| **`Authorization`** | 클라이언트가 가지고 있는 서버에 대한 권한(인증) 정보 |
| **`Origin`** | 요청이 시작된 주소 정보 (CORS 방어 등에서 중요하게 쓰임) |
| **`Cookie`** | 서버에 의해 설정된 클라이언트의 쿠키 정보 (로그인 상태 유지 등) |


Body

클라이언트각 서버에 전달하는 실제 데이터
주로 POST, PUT 요청에 사용

응답메시지의 구성요소

Status Line

HTTP/1.1 200 OK
HTTP 버전 / HTTP 상태 코드 / 상태 메시지

HTTP 버전
HTTP 상태코드 
- 클라이언트의 요청의 상태에 대한 서버의 응답을 나타내는 3자리 코드
상태 메시지
- 응답 상태에 대한 설명

Header


### HTTP 주요 응답 헤더 (Response Headers)

| 헤더 필드 | 설명 |
| :--- | :--- |
| **`Location`** | 리다이렉션 해야 하는 페이지의 URL |
| **`Server`** | 서버에서 사용하는 소프트웨어 정보 (예: Apache, Nginx) |
| **`Age`** | 응답이 생성되고 지난 초 단위의 시간 |
| **`Referrer-Policy`** | 요청 메시지에 포함할 Referer 정보의 양을 제어 |
| **`WWW-Authenticate`** | 클라이언트가 서버에 인증해야 하는 방식 요구 (보통 401 에러와 함께 옴) |
| **`Proxy-Authenticate`** | 클라이언트가 프록시 서버로 인증 정보를 보내는 방식 요구 |



HTTP 상태 코드

HTTP 상태코드 : 클라이언트의 요청에 대한 서버의 응답을 나타내는 3자리 숫자코드
### HTTP 상태 코드 (Status Code) 요약

| 코드 대분류 | 의미 | 설명 |
| :--- | :--- | :--- |
| **1xx** | 정보 응답 (Informational) | 요청을 받아들여 처리 중임을 나타냄 |
| **2xx** | 성공 (Successful) | 요청이 성공적으로 접수되고 처리됨 |
| **3xx** | 리다이렉션 (Redirection) | 요청 완료를 위해 클라이언트의 추가 작업(다른 주소로 이동 등)이 필요함 |
| **4xx** | 클라이언트 오류 (Client Error) | 클라이언트의 요청 문법이 잘못되었거나, 요청을 처리할 수 없음 |
| **5xx** | 서버 오류 (Server Error) | 서버 측의 문제로 인해 정상적인 요청을 처리하지 못함 |

각 카테고리의 대표 넘버


### HTTP 상태 코드: 1xx (정보 응답)
**"요청을 받았으며 프로세스를 계속함"**

| 코드 | 설명 |
| :--- | :--- |
| **100** | **Continue**<br>- 클라이언트가 요청의 첫 번째 부분을 전송했으며, 서버가 나머지 요청을 계속해서 전송하라고 응답 |
| **101** | **Switching Protocol**<br>- 클라이언트가 프로토콜 전환을 요청했으며, 서버가 이를 승인 |
| **102** | **Processing**<br>- 서버가 요청을 수신하여 처리 중이지만, 아직 완료되지 않았음을 나타냄 |
| **103** | **Checkpoint**<br>- 응답 헤더의 초기 힌트를 클라이언트에게 제공하여 리소스 로드를 가속화 |

### HTTP 상태 코드: 2xx (성공)
**"요청을 성공적으로 수신하고 실행함"**

| 코드 | 설명 |
| :--- | :--- |
| **200** | **OK**<br>- 요청이 성공적으로 처리되었음을 나타냄 |
| **201** | **Created**<br>- 요청이 성공적으로 처리되었으며, 새로운 리소스가 생성되었음을 나타냄 |
| **202** | **Accepted**<br>- 요청이 수락되었지만, 아직 처리되지 않았음을 나타냄 |
| **203** | **Non-Authoritative Information**<br>- 요청이 성공적으로 처리되었지만, 응답 헤더 정보가 원본 서버가 아닌 다른 소스로부터 왔음을 나타냄 |
| **204** | **No Content**<br>- 요청이 성공적으로 처리되었지만, 반환할 내용이 없음을 나타냄 |


### HTTP 상태 코드: 3xx (리다이렉션)
**"요청 완료를 위해 추가 작업 조치 필요"**

| 코드 | 설명 |
| :--- | :--- |
| **300** | **Multiple Choices**<br>- 요청한 리소스에 대한 여러 가지 선택지가 있음을 나타냄 |
| **301** | **Moved Permanently**<br>- 요청한 리소스가 영구적으로 새로운 URL로 이동되었음을 나타냄 |
| **302** | **Found**<br>- 요청한 리소스가 임시로 다른 URL에 위치함을 나타냄 |
| **304** | **Not Modified**<br>- 요청한 리소스가 수정되지 않았으며, 클라이언트의 캐시된 버전을 사용할 수 있음을 나타냄 |

### HTTP 상태 코드: 4xx (클라이언트 오류)
**"클라이언트의 요청 오류"**

| 코드      | 설명                                                     |
| :------ | :----------------------------------------------------- |
| **400** | **Bad Request**<br>- 클라이언트의 요청이 잘못되었음을 나타냄             |
| **401** | **Unauthorized**<br>- 인증이 필요하지만 제공되지 않았거나 유효하지 않음을 나타냄 |
| **403** | **Forbidden**<br>- 서버가 요청을 이해했지만, 권한이 없어서 요청을 거부함      |
| **404** | **Not Found**<br>- 요청한 리소스를 찾을 수 없음을 나타냄               |
| **405** | **Method Not Allowed**<br>- 요청한 메서드가 허용되지 않음을 나타냄      |

### HTTP 상태 코드: 5xx (서버 오류)
**"서버의 응답 오류"**

| 코드      | 설명                                                                  |
| :------ | :------------------------------------------------------------------ |
| **500** | **Internal Server Error**<br>- 서버가 요청을 처리하는 도중 예기치 않은 상태에 도달했음을 나타냄 |
| **501** | **Not Implemented**<br>- 서버가 요청을 수행할 방법을 지원하지 않음을 나타냄               |
| **502** | **Bad Gateway**<br>- 게이트웨이 또는 프록시 서버가 상위 서버로부터 잘못된 응답을 받았음을 나타냄     |
| **503** | **Service Unavailable**<br>- 서버가 일시적으로 요청을 처리할 수 없음을 나타냄            |


REST API 개요

REST
	Representational State Transfer
	Http 프로토콜에 기반하여 자원을 관리하는 방식을 정의
	자원을 이름으로 식별하여 자원의 상태 정보를 주고 받음
API
	Application Programing Interface
	시스템 간 상호작용을 위한 인터페이스
	레스토랑의 점원


즉, REST API란


REST의 원리(HTTP 프로토콜)을 따르는 API이다

RESTAPI의 특징

- 클라이언트 - 서버(Client-Server) 구조
	- 사용자 인터페이스와 비즈니스 로직/데이터 저장의 분리
- 무상태(Stateless)
	- 각 요청은 독립적, 서버에 상태 정보 유지X
- 캐시 처리 가능
	- 응답은 캐싱 가능해야함
- 계층화 시스템
	- 클라이언트는 서버를 직접 호출하지않음 -> 다중 계층 허용(로드밸러ㄴ싱, 보안, 암호화 등)
- 인터페이스 일관성
	- 효율적 아키텍처를 위한 일관된 인터페이스
- 코드 온 디멘드
	- 서버는 실행 가능 코드 형태로 기능을 전홍할 수 있음


이런 원칙을 잘 준수한 API를 RESTFUL API라고 한다!


---

REST API의 구성요소


1. 자원(Resources) - URL
	 모든 자원에 고유한 id가 존재
	 데이터 베이스의 엔티티나 정보의 단위
		 사용자, 제품, 주문 등
		 

URI란? Uniform Resource Identifier
URI는 정보의 자원을 표현함, 즉 지원식별자이다.


URI의 구조

### URL(URI) 구조 분석
**예시:** `http://www.example.com:8080/resource/page.html?param1=value1&param2=value2#section1`

| 구성 요소 | 예시 부분 | 설명 |
| :--- | :--- | :--- |
| **Scheme** | `http` | 리소스에 접근하기 위한 프로토콜 (웹은 주로 http, https) |
| **Host** | `www.example.com` | 리소스를 호스팅하는 서버 이름(도메인) 또는 IP 주소 |
| **Port** | `8080` | 서버에서 리소스에 접근하기 위해 사용되는 게이트 (웹 기본값: HTTP 80, HTTPS 443) |
| **Path** | `/resource/page.html` | 호스트 내에서 리소스의 정확한 위치 (폴더/파일 경로) |
| **Query Parameters** | `?param1=value1...` | 리소스에 전달되는 추가 정보 (주로 검색어나 필터링 옵션) |
| **Fragment Identifier** | `#section1` | 부가적인 식별자 (해당 웹 페이지 내의 특정 스크롤 위치 지정) |


2. 행위(Verbs) - HTTP 메서드
	자원에 대한 조작을 정의함



| 메서드 (Method + URL) | 역할 |
| :--- | :--- |
| **[GET]** `/users/{userId}` | 특정 사용자의 상세 정보를 **조회** (Read) |
| **[POST]** `/users` | 특정 사용자를 **등록** (Create) |
| **[PUT]** `/users/{userId}` | 특정 사용자의 정보를 **수정** (Update) |
| **[DELETE]** `/users/{userId}` | 특정 사용자의 정보를 **삭제** (Delete) |

이것들은 모두 일관적이여야한다!

> [!info]- RESTful API URL 의 일관성
> 1. **리소스명은 동사보다는 명사를 사용한다.**
> 2. **리소스에 대한 행위는 HTTP 메서드로 표현한다.**
> 3. **URL 경로에는 소문자를 사용한다.**
> 4. **계층 관계는 슬래시(`/`)로 구분한다.**
> 5. **마지막 문자로 슬래시(`/`)를 사용하지 않는다.**
> 6. **의미 구분을 위해서는 하이픈(`-`)을 사용하고 언더바(`_`)를 사용하지 않는다.**



3. 표현(Representations) - HTTP 메시지 페이로드
	클라이언트가 자원의 상태에 대한 조작을 요청 -> 서버응답
	자원은 JSON, XML, TEXT 등 여러 형태를 가질 수 있음

JSON이 보통 가장 많이 쓰인다.
경량의 데이터 교환형식, 프로그래밍 언어 독립적.
키/값 쌍의 집합 구조이다.


REST API의 기본 설계 원칙

1. 자원 식별
	인터넷에 있는 모든것은 고유 주소(URI)ㄹㄹ 가져야한다
	자원이 URI로 식별되어야한다, 특정 자원은 오직 하나으 ㅣURL에 매핑되어야한다
2. 표현을 통한 자원의 조작
	웹 페이지의 정보를 조작할 떄 해당 정보는 XML, JSON 같은 형식으로 수성된다
	이같이 특정 형식을 통해 정보에 대한 조회, 수정, 삭제등을 수행해야한다
	URL을 바꾸지 않고도 표현을 통해 리소스를 조작 가능해야한다
3. 자기서술 적 메시지
	메시지가 자기 자신을 어떻게 처리해야할지 설명해야한다
	수신하는 쪽에서 별도의 설명 없이도 해당 메시지를 이해하고 처리할 수 있도록 정의
4. HATEOAS(Hypermedia As The Engine of Application State)
	웹 페이지가 다른 페이지나 상태로 넘어갈 수 있도록 링크를 제공해야한다
	링크를 통해 사용자나 프로그램이 다음에할 수 있는 동작을 유추 가능하다



---

데이터베이스 소개

데이터란? 현실 세계에서 수집된 단순한 사실과 값들을 모아 놓은 것

정보는 데이터를 특정 목적에 의해 해석하거나 가공한 형태

데이터 베이스란

한 조직 안에서 여러 사용자와 응용프로그램이 공동으로 사용하는데이터들을 통합하여 저장하고 운영하는 데이터

데이터베이스의 종류

1. RDB(Relational DB)
	데이터를 행과 열을 가지는 테이블로 표현
	테이블 간의 관계를 이용해 데이터를 효과적으로 관리하여 데이터의 무결성을 보장
	정의된 테이블(스키마)에 맞게 데이터가 삽입되므로 데이터의 안정성을 보장
	데이터의 구조가 일관적인 경우에 주로 사용

	RDBMS의 종류
		MySQL
		PostgreSQL
		MariaDB

2. NoSQL(Not only SQL)
	데이터를 저장할 수 있는 유형의 제한이없음
	새로운 유형의 데이터를 추가하기 용이
	데이터의 구조가 일관적이지 않고 자주 변경되는 경우에 적합
	대용량의 데이터를 더 빠르게 처리할 수 있음(이미지, 음성 등)

	NoSQL의 종류
		mongoDB
		redis
		cassandra

가장 큰 차이는
관계!!!!

---

관계형 데이터 베이스의 구성요소

테이블은 행, 열로 구성
속성은 데이터의 특성을 나타내는 가장 작은 논리적 단위
튜플은 속성이 모여 구성된 각각ㄱ의 행
속성이 가질 수 있는 값의 집합을 도메인이라 함

관계
	관계가 ㅇ벗으면 주문번호등을 ID,이름, 주소와 함께 관리해야한다.
	하지만 관계가 있으니 하나의 속성으로 테이블을 연결해서 데이터들을 효과적으로 관리 가능하다.


관계형 데이터 베이스 만들기

테이블 정의하기

```
CREATE TABLE customer(
	id VARCHAR(10),
	name VARCHARE(10),
	address VARCHAR(30)
);
```

CREATE TABLE 테이블명(속성1 데이터타입1, 속성2 데이터타입 2,...);

정의한 테이블 확인하기

데이터베이스의 테이블 목록을 확인

```
SHOW TABLES; 

```

테이블의 구조를 확인
```
DESC customer;
```

SQL 문법의 기본 작성 규칙

- SQL 문법은 대문자로 작성  권장
- 테이블명, 속성명은 소문자로 작성 권장
- 이름은 항상 의미가 잘 드러나도록 작성
- 이름에 여러 단어를 혼합하는 경우 "_"를 이용해서 구분
- "--"을 이용해 주석을 나타낼 수 있음
- 명령어 ㄷ끝에는 세미콜론(;) 작성

데이터 삽입하기

```
INSERT INTO customer (id, name, address)
VALUES('kmax6', '김민준', '서울시 관악구 신림동');

-- 속성의 순서는 중요하지않는다

INSERT INTO customer (name, address, id)
VALUSE('이서연', '서울시 동작구 대방동', 'flykit');

-- 모든 속성을 순서대로 입력하는 경우에 속성 목록은 생략가능
INSERT INTO custome
VALUES('freeman123', '박서준', '서울시 관악구 신림동');

```

INSERT INTO 테이블면 (속성1, 속성2,...) VALUES(속성값1, 속성값2, ...);

속성값을 넣지않으면 default 값인 NULL이 삽입됨



데이터 출력하기

```
SELECT id, name, addredd FROM customer;

-- 출력하고 싶은 속성 조정 가능
SELECT address, name FROM customer;

--SELECT * FROM customer;
```

SELECT 속성1, 속성2,... FROM 테이블명


데이터베이스 정의어

SQL(Structured Query Language) 

관계형 데이터베이스를 활용하기 위해 사용하는 표준어

SQL의 종류

1. 데이터 정의어(DDL, Data Definition Language): 테이블과 같은 데이터 구조 정의(이번에 다룰 질의어)
    
2. 데이터 조작어(DML , Data Manipulation Language): 데이터 조회 및 검색
    
3.  데이터 제어어(DCL , Data Control Language): 데이터베이스에 접근하는 권한 관리


```
CREATE TABLE customer(
    id      VARCHAR(10) NOT NULL, --꼭 들어가야하는 데이터로 정의
    name    VARCHAR(10) NOT NULL,
    address VARCHAR(30) NULL
);

```

CREATE TABLE 테이블명(속성1 데이터타입1 제약조선, 속성 2 데이터타입 2 제약조건2, ...);


### 데이터베이스 주요 자료형 (Data Types)

| 자료형            | 의미                                          |
| :------------- | :------------------------------------------ |
| **VARCHAR(n)** | nBytes 크기의 **가변 길이** 문자열 데이터                |
| **INT**        | 정수형 숫자 데이터 (4Bytes)                         |
| **FLOAT**      | 4Bytes 크기의 부동 소수점 데이터                       |
| **DATETIME**   | 날짜와 시간 형태의 기간 데이터 <br>(YYYY-MM-DD HH:MM:SS) |


테이블 수정하기

```
-- 컬럼 추가
ALTER TABLE custimer ADD COLUMN birthday DATE NULL;
-- 컬럼 수정
ALTER TABLE customer MODIFY COLUMN id varchar(15) NULL:
-- 컬럼 이름 변경
ALTER TABLE custimer CHANGE COLUMN name korean)name varchar(10) NOT NULL;
-- 컬럼 삭제
ALTER TABLE custimer DROP COLMN adderss;
-- 테이블 이름 변경
ALTER TABLE customer RENAME member;

```


컬럼 추가: `ALTER TABLE 테이블명 ADD COLUMN 컬럼명 데이터타입 제약조건` 
컬럼 수정: `ALTER TABLE 테이블명 MODIFY COLUMN 컬럼명 데이터타입 제약조건` 
컬럼 이름 변경: `ALTER TABLE 테이블명 CHANGE COLUMN 기존컬럼명 새로운컬럼명 데이터타입 제약조건` 
컬럼 삭제: `ALTER TABLE 테이블명 DROP COLUMN 컬럼명` 
테이블 이름 변경: `ALTER TABLE 기존테이블명 RENAME 새로운테이블명`

테이블 삭제하기

``
```
DROP TABLE member;
```

DROP TABLE 테이블 명


---

데이터베이스 구정하기

제약조건

1. NOT NULL
	   NULL 값 비허용
	   데이터를 입력하지않는다면 에러가 발생한다
	   아무것도 명시하지않는 경우에 기본값은 널 값 허용
2.  UNIQUE
		중복되는 값을 비허용함
		똑같은 값이 있으면 에러가 발생함
		NULL 값은 비교가 불가능하여 중복되어도 에러가 발생하지않음
3. DEFAULT
		기본값을 설정
		아무런 값을 지정하지않으면 DEFAULT 값으로 설정됨
		
4. CHECK
		값의 범위를 제한하여 특정 값만 허용
		제한한 값이 아닌 경우 에러 발생함
	
 CONSTRAINT
	제약 조건 이름 정의: CONSTRAINT 제약조건 이름 제약조건(UNIQUE, CHECK,...) (적용할 속성);
	생성된 제약 조건 확인 SELECT * FROM information_schema.table_constraints;
	NOT NULL, DEFAULT는 사용 불가능!!!!


제약 조건 추가 : ALTER TABLE 테이블 명 ADD CONSTRAINT 제약 조건 이름 제약조건 (속성);
DEFAULT 제약 조건 수정: ALTER TABLE 테이블 명 ALTER 속성 SET DEFAULT 기본값;


제약 조건 삭제 : ALTER TABLE 테이블명 DROP CONSTRAINT 제약조건 이름;
DEFAULT 제약 조건 삭제 :ALTER TABLE 테이블명 ALTER 속성 DROP DEFAULT



데이터 베이스에서의 키

조건에 만족하는 튜플을 찾거나, 정럴할떄 기준이 되는 속성을 의미한다

기본키 (PK, Primary Key)
	서로 다른 튜플을 유일하게 실별할 수 있는 기준이 되는속성
	중복되는 값을 가질수없음
	널 값을 가질 수 없음
	테이블 당 1개만 설명


외래 키(FK, Foreign Key)
	다른 테이블의 기본키를 참조하는 속성으로 테이블의 관계를 정의
	참조하는 테이블의 키본키에 없는 값은 지정할 수 없음

후보 키
	기본 키가 될 수 있는 키로 유일성과 최소성을 만족(후보 키는 기본키의 상위)
대체 키
	후보 키중에 기본키가 아닌 키(대체 키는 후보키의 하위)
슈퍼 키
	튜플을 식별할수 있는 유일성은 만족하지만 최소성은 만족하지않는 키

SQL에서의 PK,FK 설정방법

PK, FK도 제약조건의 일부이다.

기본키

```
id VARCHAR(10) PRIMARY KEY

FOREIGN KET (customer_id) REFERENCES customer(id)
```

기본 키 설정 PRIMARY KEY

외래키 FOREIGN KEY(참조할 속성) REFERENCES 참조되는 테이블(참조되는 속성)


무결성 제약 조건
	개체 무결성
		기본키는 널 값과 중복된 값을 가질 수 없음
	참조 무결성
		외래키는 널이거나 참조되는 릴레이션의 기본키 값과 동일


		도메인 무결성
			특성 속성값은 그 속성이 정의된 도메인에 속한 값이여야한다
		NULL 무결성
				특성 속성값은 널 값을 가질 수 없음
		 고유 무결성
				각 튜플이 가지는 속성값들은 서로 달라야 함
		키 무결성
				테이블에 최소 한개 이상의 키 존재

데이터 모델링

데이터 모델링
	현실세계의 데이터를 단순화, 추상화 하여 표현한 모델
	- 개체(Entity)
		데이터로 표현하고자 하는 현실 세계의 개념이나 정보의 단위(요구사항의 명사)
	- 속성(Attribute)
		개체에 대한 정보
	- 관계(Relationship)
		개체 간의 연관성(요구사항에서의 동사)



데이터 모델링 과정
	현실세계를 추상적 개념으로 표현화 하는 과정
	
	개념적 설계
		DBMS가 처리할 수 있는 데이터 구조(스키마) 를 설계
	논리적 설계
		DBMS에 테이블을 저장할 구조를 설계
	물리적 설계


ER다이어그램(Peter Chen 방식)

ERD, Entity Relationship Diagram(개체 - 관계 다이어그램)
	현실세계의 데이터를 개체와 관계 형태의 다이어그램으로 나타내는것

![[Pasted image 20260524215619.png]]

![[Pasted image 20260524220213.png]]

하나의 데이터가 다른 테이블의 몇개의 데이터와 관계를 가지는지 나타낸다

N : M 관계는 테이블로 표기하며 각 개체의 기본키를 외래 키로 가진다



ER다이어그램(IE)
	Information Engineering
![[Pasted image 20260524221309.png]]

키를 나타내며 데이터 타입과 제약 조건 표기, 관계를 세부적으로 나타냄


![[Pasted image 20260524221414.png]]